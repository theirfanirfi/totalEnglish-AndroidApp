<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="totalenglish.irfanullah.com.totalenglish">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app1_id" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/FullScreenTheme"></activity>
        <activity
            android:name=".SplashScreen"
            android:theme="@style/FullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LanguageActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".LevelActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".LessonActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".LessonDetail"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".PagerTesting"
            android:theme="@style/FullScreenTheme"></activity>
        <activity android:name=".Main2Activity" />
        <activity
            android:name=".QuizActivity"
            android:theme="@style/FullScreenTheme"></activity>
        <activity android:name=".GoPremiumActivity" android:theme="@style/FullScreenTheme"></activity>
    </application>

</manifest>